import styled from 'styled-components';

export const Container = styled.div`
  #closeModalButton {
    position: absolute;
    top: 0.8rem;
    right: 0.8rem;
    cursor: pointer;
    color: var(--green);
    width: 1.8rem;
    height: 1.8rem;
  }
`;